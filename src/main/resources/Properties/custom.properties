# SHAFT user overrides (loaded from src/main/resources/properties/custom.properties)
# Requires: -DpropertiesFolderPath=src/main/resources/properties (Maven surefire sets this)

telemetry.enabled=false
